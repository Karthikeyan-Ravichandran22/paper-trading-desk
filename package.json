{
  "name": "angel-one-paper-trading",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173",
    "test": "cd backend && .venv/bin/pytest app/tests -v",
    "build": "cd frontend && npm run build"
  }
}
